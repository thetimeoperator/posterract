# Short-form video

Structure one scene as hook, development, proof/demo, and payoff sequences. Keep scene time continuous; sequences provide organization rather than separate videos.

Expose hook copy, accent color, and CTA as inspector variables when the user wants variants. Duplicate the top-level scene only when the project truly needs independently exportable variants.
