# Podcast clip

Use one portrait scene. Put the selected local excerpt in a primary sequence, speaker framing in groups, captions above the platform-safe bottom area, and a subtle progress treatment only when requested.

Use waveform silence spans to select trims. Preserve source offsets when splitting. Verify lip/audio synchronization in captures around every split and transition.
