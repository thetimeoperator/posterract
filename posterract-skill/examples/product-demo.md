# Product demo

Use one scene with sequences for setup, interaction, result, and CTA. Keep screen footage as primary media and use groups for callouts, cursor treatments, and labels.

Probe the screen recording, inspect a filmstrip, choose exact interaction times, then add callouts. Capture before, during, and after every important interaction so the visual claim is actually visible.
