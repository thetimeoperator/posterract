# Assets

Place your own reference image here if you want to build a new photo avatar from a local file.

Recommended path:

- `reference-image.jpg`

If you already have a render-ready HeyGen talking photo id or avatar look id, you can skip the local image and just configure the ids in:

- `config/video.json`
- `.env`
